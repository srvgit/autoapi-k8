apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../base

resources:
  - ../base

patches:
  - environment-patch.yaml
  - environment-patch-hpa.yaml
  - environment-patch-vpa.yaml  # Include this line for the VPA patch

commonLabels:
  env: dev
