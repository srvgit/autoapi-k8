apiVersion: apps/v1
kind: Deployment
metadata:
  name: generic-graphlet
spec:
  replicas: 2
  template:
    spec:
      magePullSecrets:
      - name: regcred
      containers:
      - name: auto-api-graphlet
        image: docker1460/autoapi:latest
        env:
        - name: ENVIRONMENT
          value: dev
        livenessProbe:
          httpGet:
            path: /healthz
            port: 9090
        readinessProbe:
          httpGet:
            path: /readiness
            port: 9090